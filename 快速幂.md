```cpp
using i64 = long long;

constexpr i64 mod = 1e9 + 7;
i64 qpow(i64 a, i64 b) {
    i64 result = 1;
    a = a % mod;
    while (b > 0) {
        if (b & 1) {
            result = (result * a) % mod;
        }
        a = (a * a) % mod;
        b >>= 1;
    }
    return result;
}
```