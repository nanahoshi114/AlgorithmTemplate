```cpp
auto polygon_size(const std::vector<Point> &pts) { //需要保证点按逆时针顺序给出
    int n = pts.size();
    i64 sum = 0;
    for (int i = 1; i < n; ++i) {
        sum += a[i].y * a[i - 1].x - a[i].x * a[i - 1].y;
    }
    sum += a[0].y * a[n - 1].x - a[0].x * a[n - 1].y;
    return std::abs(sum) / 2;
}
```