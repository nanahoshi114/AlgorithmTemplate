```c
static char bufferI[1 << 23], *pI = bufferI, bufferO[1 << 23], *pO = bufferO;
typedef int64_t i64;
static inline i64 readi64() {
    i64 sign = 1;
    while (*pI != '\0' && !isdigit(*pI)) { 
        if (*pI == '-') sign = -1;
        ++pI;
    }
    i64 res = 0;
    while (isdigit(*pI)) {
        res = res * 10 + (*pI++ - '0');
    }
    return res * sign;
}
static inline int reads(char *s, int (*splitFunc)(int)) {
    int len = 0;
    while (*pI != '\0' && !splitFunc(*pI)) { ++pI; }
    while (splitFunc(*pI)) { *s++ = *pI++; ++len; }
    *s = '\0';
    return len;
}
static inline void prints(char *s) {
    while (*s) { *pO++ = *s++; }
}
static inline void printi64(i64 x) {
    if (x / 10) {
        printi64(x / 10);
    } else if (x < 0) {
        *pO++ = '-';
    }
    *pO++ = (x < 0 ? -x : x) % 10 + '0';
}
```
```cpp
#include <cstdio>
#include <cctype>
char fast_getchar() {
	static char buf[1 << 20];
	static char *pos = buf, *end = buf;
	if (pos == end) {
		end = buf + fread(buf, 1, sizeof(buf), stdin);
		pos = buf;
		if (pos == end) return EOF;
	}
	return *pos++;
}
template <typename T>
T read(){
    T sum = 0, fl = 1;
    int ch = getchar();
    for(; !isdigit(ch); ch = getchar()){
        if(ch == '-') fl = -1;
    }
    for(; isdigit(ch); ch = getchar()){
        sum = sum * 10 + ch - '0';
    }
    return sum * fl;
}
struct istream{
	template<typename T>
	istream operator>>(T &x){
		x = 0;
		T fl = 1;
		int ch = getchar();
		for(; !isdigit(ch); ch = getchar()){
			if(ch == '-') fl = -1;
		}
		for(; isdigit(ch); ch = getchar()){
			x = x * 10 + ch - '0';
		}
		return *this;
	}	
} cin;
void read(auto& x) {
    using x_type = std::remove_cvref_t<decltype(x)>;
    if constexpr (std::is_same<x_type, std::string>::value || std::is_same<x_type, char*>::value) {
        x = fr.fast_gets();
    } else if constexpr (std::is_arithmetic<x_type>::value) {
        x_type fl = 1;
        x = 0;
        int ch = fr.fast_getchar();
        for(; !isdigit(ch); ch = fr.fast_getchar()){
            if (ch == '-') fl = -1;
        }
        for(; isdigit(ch); ch = fr.fast_getchar()){
            x = x * 10 + ch - '0';
        }
        x *= fl;
    }
}
```